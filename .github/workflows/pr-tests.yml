name: PR Tests

on:
  pull_request:
    branches:
      - main
      - master

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build unit-test image from repo-native Dockerfile
        run: |
          docker build --build-arg BUILD_TESTING=ON \
            --target owsub-build -t userportal-unit-tests .

      - name: Run native C++ unit tests
        run: docker run --rm userportal-unit-tests sh -lc "cd /owsub/cmake-build && ctest --output-on-failure"

  integration-tests:
    name: Build & Integration Tests
    runs-on: ubuntu-22.04
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_USER: root
          POSTGRES_PASSWORD: password
          POSTGRES_DB: fakeprov
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Verify log redaction patterns
        run: |
          chmod +x test_scripts/ci/sanitize_logs.sh test_scripts/ci/test_sanitize_logs.sh
          ./test_scripts/ci/test_sanitize_logs.sh

      - name: Build UserPortal Docker Image
        run: |
          echo "Building UserPortal using repo-native Dockerfile..."
          docker build -t test-userportal .

      - name: Start Fake Downstream Services
        run: |
          echo "Installing Python Postgres driver..."
          pip3 install psycopg2-binary
          echo "Starting fake Parental Control, Prov, GW, and Sec services..."
          python3 test_scripts/fakes/fake_downstream_services.py > fake_server.log 2>&1 &
          sleep 2

      - name: Prepare CI runtime directory on the runner
        run: |
          mkdir -p ci-runtime/certs ci-runtime/data ci-runtime/logs
          echo '{}' > ci-runtime/data/registry.json
          openssl req -x509 -newkey rsa:2048 \
            -keyout ci-runtime/certs/restapi-key.pem \
            -out ci-runtime/certs/restapi-cert.pem \
            -days 1 \
            -subj "/CN=localhost" \
            -passout pass:mypassword
          cp ci-runtime/certs/restapi-cert.pem ci-runtime/certs/restapi-ca.pem
          chmod 644 ci-runtime/certs/restapi-key.pem ci-runtime/certs/restapi-cert.pem ci-runtime/certs/restapi-ca.pem

      - name: Validate generated certs
        run: |
          openssl rsa -in ci-runtime/certs/restapi-key.pem -passin pass:mypassword -check -noout
          openssl x509 -in ci-runtime/certs/restapi-cert.pem -noout -subject -issuer
          ls -l ci-runtime/certs

      - name: Render config on the runner
        run: |
          export RESTAPI_HOST_ROOTCA=/owsub-data/certs/restapi-ca.pem
          export RESTAPI_HOST_PORT=16006
          export RESTAPI_HOST_CERT=/owsub-data/certs/restapi-cert.pem
          export RESTAPI_HOST_KEY=/owsub-data/certs/restapi-key.pem
          export RESTAPI_HOST_KEY_PASSWORD=mypassword
          export INTERNAL_RESTAPI_HOST_ROOTCA=/owsub-data/certs/restapi-ca.pem
          export INTERNAL_RESTAPI_HOST_PORT=17006
          export INTERNAL_RESTAPI_HOST_CERT=/owsub-data/certs/restapi-cert.pem
          export INTERNAL_RESTAPI_HOST_KEY=/owsub-data/certs/restapi-key.pem
          export INTERNAL_RESTAPI_HOST_KEY_PASSWORD=mypassword
          export SERVICE_KEY=/owsub-data/certs/restapi-key.pem
          export SERVICE_KEY_PASSWORD=mypassword
          export SYSTEM_DATA=/owsub-data/data
          export SYSTEM_URI_PRIVATE=https://localhost:17006
          export SYSTEM_URI_PUBLIC=https://localhost:16006
          export SYSTEM_URI_UI=http://localhost
          export KAFKA_ENABLE=false
          export KAFKA_BROKERLIST=localhost:9092
          export KAFKA_SSL_CA_LOCATION=
          export KAFKA_SSL_CERTIFICATE_LOCATION=
          export KAFKA_SSL_KEY_LOCATION=
          export KAFKA_SSL_KEY_PASSWORD=
          export STORAGE_TYPE=sqlite
          export STORAGE_TYPE_POSTGRESQL_HOST=localhost
          export STORAGE_TYPE_POSTGRESQL_USERNAME=owsub
          export STORAGE_TYPE_POSTGRESQL_PASSWORD=owsub
          export STORAGE_TYPE_POSTGRESQL_DATABASE=owsub
          export STORAGE_TYPE_POSTGRESQL_PORT=5432
          export STORAGE_TYPE_MYSQL_HOST=localhost
          export STORAGE_TYPE_MYSQL_USERNAME=owsub
          export STORAGE_TYPE_MYSQL_PASSWORD=owsub
          export STORAGE_TYPE_MYSQL_DATABASE=owsub
          export STORAGE_TYPE_MYSQL_PORT=3306
          export LOGGING_LEVEL=debug
          export SECURITY_RESTAPI_DISABLE=false
          envsubst < owsub.properties.tmpl > ci-runtime/owsub.properties
          echo "logging.websocket = true" >> ci-runtime/owsub.properties

      - name: Start UserPortal
        run: |
          echo "Starting UserPortal connected to fakes..."
          docker run -d --name userportal --network host \
            -v "$PWD/ci-runtime:/owsub-data" \
            -e RUN_CHOWN=true \
            -e CI_FAKE_EXTERNAL_SERVICES=1 \
            -e FAKE_EXTERNAL_SERVICE_MANGO_PARENTAL_CONTROL=http://127.0.0.1:8080 \
            -e FAKE_EXTERNAL_SERVICE_NWTOPOLOGY=http://127.0.0.1:8080 \
            -e FAKE_EXTERNAL_SERVICE_OWPROV=http://127.0.0.1:8080 \
            -e FAKE_EXTERNAL_SERVICE_OWGW=http://127.0.0.1:8080 \
            -e FAKE_EXTERNAL_SERVICE_OWSEC=http://127.0.0.1:8080 \
            test-userportal

          echo "Waiting for UserPortal to be ready..."
          READY=0
          for i in {1..30}; do
            if curl -ks https://localhost:16006/ > /dev/null; then
              echo "UserPortal is up!"
              READY=1
              break
            fi
            sleep 1
          done

          if [ "$READY" -eq 0 ]; then
            echo "UserPortal failed to start within timeout!"
            exit 1
          fi

          sleep 2

      - name: Run Groups Contract Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Python contract tests..."
          python3 test_scripts/integration/test_groups_contract.py

      - name: Run Groups Integration Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Python integration tests..."
          python3 test_scripts/integration/test_groups_api.py

      - name: Run Schedules Contract Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Schedules contract tests..."
          python3 test_scripts/integration/test_schedules_contract.py

      - name: Run Schedules Integration Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Schedules integration tests..."
          python3 test_scripts/integration/test_schedules_api.py

      - name: Run Group-Devices Contract Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Group-Devices contract tests..."
          python3 test_scripts/integration/test_group_devices_contract.py

      - name: Run Group-Devices Integration Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Group-Devices integration tests..."
          python3 test_scripts/integration/test_group_devices_api.py

      - name: Run Group-Schedules Contract Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Group-Schedules contract tests..."
          python3 test_scripts/integration/test_group_schedules_contract.py

      - name: Run Group-Schedules Integration Tests
        env:
          USERPORTAL_URL: https://localhost:16006
        run: |
          echo "Running Group-Schedules integration tests..."
          python3 test_scripts/integration/test_group_schedules_api.py

      - name: Collect and Sanitize Diagnostic Logs on Failure
        if: failure()
        run: |
          mkdir -p ci-logs
          COLLECTION_FAILED=0

          echo "Collecting bounded, sanitized UserPortal logs..."
          if ! (set -o pipefail; docker logs userportal 2>&1 | tail -500 | ./test_scripts/ci/sanitize_logs.sh > ci-logs/userportal-debug.log); then
            echo "::warning::Failed to collect or sanitize UserPortal container logs"
            COLLECTION_FAILED=1
          fi
          if [ ! -s ci-logs/userportal-debug.log ]; then
            echo "::warning::ci-logs/userportal-debug.log is empty or missing"
            COLLECTION_FAILED=1
          fi

          echo "Collecting bounded, sanitized fake server logs..."
          if [ -f fake_server.log ]; then
            if ! (set -o pipefail; tail -200 fake_server.log 2>&1 | ./test_scripts/ci/sanitize_logs.sh > ci-logs/fake-server-debug.log); then
              echo "::warning::Failed to collect or sanitize fake server logs"
              COLLECTION_FAILED=1
            fi
            if [ ! -s ci-logs/fake-server-debug.log ]; then
              echo "::warning::ci-logs/fake-server-debug.log is empty or missing"
              COLLECTION_FAILED=1
            fi
          else
            echo "::warning::fake_server.log was not found on runner"
            COLLECTION_FAILED=1
          fi

          {
            echo "UserPortal CI Diagnostic Collection Status"
            echo "Timestamp: $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            if [ -s ci-logs/userportal-debug.log ]; then
              echo "userportal-debug.log: COLLECTED ($(wc -l < ci-logs/userportal-debug.log) lines)"
            else
              echo "userportal-debug.log: FAILED or EMPTY"
            fi
            if [ -s ci-logs/fake-server-debug.log ]; then
              echo "fake-server-debug.log: COLLECTED ($(wc -l < ci-logs/fake-server-debug.log) lines)"
            elif [ ! -f fake_server.log ]; then
              echo "fake-server-debug.log: NOT_FOUND (fake_server.log was not found on runner)"
            else
              echo "fake-server-debug.log: FAILED or EMPTY"
            fi
          } > ci-logs/collection-status.txt

          if [ "$COLLECTION_FAILED" -ne 0 ]; then
            echo "::warning::Diagnostic collection failed; failure artifact may be incomplete"
          fi

          echo "=== UserPortal Logs (last 50 lines, sanitized) ==="
          tail -50 ci-logs/userportal-debug.log 2>/dev/null || true
          echo "=== Fake Server Logs (last 50 lines, sanitized) ==="
          tail -50 ci-logs/fake-server-debug.log 2>/dev/null || true
          echo "Full diagnostic logs are uploaded as artifact: userportal-ci-failure-logs"

      - name: Upload Diagnostic Logs on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: userportal-ci-failure-logs
          path: |
            ci-logs/*.log
            ci-logs/collection-status.txt
          retention-days: 7
          if-no-files-found: warn

      - name: Cleanup
        if: always()
        run: |
          docker rm -f userportal >/dev/null 2>&1 || true
